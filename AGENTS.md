## Learned User Preferences

- When auditing or changing data paths, cross-check table and column names against `prisma/schema.prisma` and `prisma/migrations`, not assumptions from older SQL scripts.
- For security and tenancy reviews, prefer concrete file references, severities, and apply-ready fixes over high-level advice alone.
- For inventory data entry (Visual, Busy, Excel): consult **`docs/AUDIT.md`** and **`docs/INVENTORY_UI_MODES_WORKFLOW.md`**; route hub saves through **`handleSaveProduct`** / **`upsertIntegratedProductAction`**; share columns/field mapping via **`lib/utils/inventoryGridColumns.js`**, **`inventoryFieldSuggestions.js`**, **`inventoryRowDefaults.js`**, **`excelProductPayload.js`**, **`inventoryRowDiff.js`** (avoid new per-mode forks); Zoho/Busy/Odoo-style autocomplete uses HTML **`datalist`** + smart row defaults from **`buildNewInventoryRow`** / **`getLastRowForDefaults`**.
- For hub, storefront, POS, public-store, and marketing mobile work: keep desktop layouts unchanged; use dual-layout with `lg:` breakpoints (`hidden lg:block` / `lg:hidden`) — compact app-style tile hubs on small screens for hub; marketing pages use shared tokens in **`lib/utils/marketingLayout.js`** and **`MarketingSection`** (`components/marketing/layout/MarketingSection.jsx`). Marketing trust/integration copy must align with **`lib/marketing/capabilities.js`** and **`docs/MARKETING_CAPABILITY_MAP.md`** (honest Available/Roadmap labels; no unverified stats); keep marketing hero copy global, not region-specific badges. Domain package **`/solutions/[slug]`** pages use one full-viewport **`DomainPackageHeroShowcase`** carousel via **`buildUnifiedPackageSlides`** (`lib/marketing/domainPackageVerticalMeta.js` — suite + channels + registration vertical presets); avoid duplicate bottom pill nav or a separate vertical slider section. **Included capabilities** on **`DomainPackageSolutionsPage`**: four cards in one `lg:grid-cols-4` row; when a package has more than four groups, use a single-row horizontal auto-scroll marquee (not wrapped multi-row grids). **`DemoStoreGallery`** hero slides: stack backdrop → `next/image` `fill` → gradients → content (backdrop must not paint over the image). For dense marketing card grids (homepage demo store gallery, similar sections): prefer CSS-only hover via parent `group` over Framer Motion; avoid `blur-2xl`, animating to `height: auto`, and per-card React hover state; use `motion-safe:` / `motion-reduce:`.
- For automotive public storefronts: keep **`auto-parts`** parts-finder isolated; use separate vertical keys for single-brand dealership (**`vehicle-dealership`**) vs portal marketplace (**`auto-marketplace`**); do not add forum/CMS, live COE APIs, or app-download placeholders unless explicitly requested.
- For public storefronts: do not reintroduce generic footer trust strips (Fast Delivery, Secure Payment, etc.) or the pre-footer store contact CTA strip — removed globally in **`StoreFooter.jsx`** / **`page.jsx`**; keep vertical-specific strips (e.g. dealership hours) only when intentional. Owners configure homepage marketing banners via **`settings.pageSections`** (**`MarketingSectionsEditor`**, **`StoreMarketingSections`**) — image-only or styled banners with placement **`after-hero` | `mid-page` | `before-footer`** on all elevated templates. Restaurant elevated dark shells (**`RESTAURANT_UI`**, `#0a0a0a`) must not use **`text-gray-900`** / **`text-zinc-900`** without a light card wrapper; cart drawer uses **`RESTAURANT_CART_DRAWER_UI`** (dark item list + light checkout footer via **`RESTAURANT_CHECKOUT_UI`**).
- For store Settings media uploads (hero carousel, product images, marketing banners): use client-side WebP optimization via **`lib/utils/optimizeImageClient.js`** and **`POST /api/upload/product-image`** (`purpose`: hero | product | banner | logo); hero supports multiple slides; inventory/product uploads allow up to **3** images on variant-capable domains.
- For registration and onboarding: prioritize credible day-one storefront + inventory wiring across registration, hub, and public store with strict **`business_id`** tenancy; extend rich-catalog verticals via **`REGISTRATION_RICH_CATALOG_VERTICALS`** and **`PK_CLOTHING_REGISTRATION_VERTICALS`** + **`shouldSeedRichCatalogOnRegistration`** in **`lib/onboarding/registrationRichVerticals.js`** rather than one-off **`createBusiness`** branches (**`clothing-commerce`** **`domainPackageKey`** seeds PK clothing rich catalog regardless of country); PK clothing tenants also ship full editorial homepage sections via **`buildDefaultFashionEditorialStorefrontSeed`** (**`settings.storefront.fashion`**) in **`registrationStorefrontDefaults`**; scale vertical commercial SKUs via **`lib/config/domainPackages.js`** + **`domainPackageKey`** / **`?package=`** (not per-vertical registration forks).
- Typography and user-facing copy: use Open Sans via **`lib/fonts.ts`** and scale tokens in **`lib/utils/typography.js`**; avoid em dashes in UI copy (**`lib/utils/copyTypography.js`**, **`CopyText`**); prefer **`font-semibold`** over **`font-black`** / **`font-extrabold`** for headings; use **`MARKETING_STAT_VALUE`** / **`tabular-nums`** for KPI and price figures (Zoho/Busy-style professionalism); bulk hub typography via **`scripts/normalize-hub-typography.mjs`**, dash cleanup via **`scripts/normalize-copy-dashes.mjs`**.
- For POS and Inventory mobile barcode/QR scanning: reuse shared **`PosCameraScanner`** (**`html5-qrcode`**) across tabs; do not fork separate camera scanner implementations. Hub/POS/invoice scan lookup flows through **`lib/utils/productScanLookup.js`**, **`lib/utils/barcodeUtils.js`** (GS1/UPC normalization), **`lib/hooks/useProductScan.js`**, and tenant **`lookupProductByScanCodeAction`** (**`lib/actions/standard/inventory/lookup.js`**) when the client catalog miss; UI entry via **`BarcodeScanTrigger`** / **`BarcodeFieldInput`**; gate camera with **`canUseBarcodeScan`** (**`lib/utils/barcodeAccess.js`**, plan **`barcode_scanning`**); run **`bun run verify:barcode-wiring`** after barcode edits.
- For hub inventory domain columns/defaults: use **`resolveInventoryDomainFeatures`** (**`lib/utils/inventoryDomainFeatures.js`**) + **`getDomainKnowledgeForBusiness`** for batch/serial/expiry flags and **`domain_data`** columns across Visual/Busy/Excel/mobile; run **`bun run verify:inventory-domains`** after inventory-domain edits.
- For public storefront commerce: prioritize smooth cross-domain order flow with no checkout roadblocks; demo homepage and **`/products`** must show accurate tenant DB inventory (UUID rows), not **`catalog_preview`** seed placeholders in orderable UI.

## Learned Workspace Facts

- **Next.js App Router**: files with top-level **`'use server'`** export only **async** Server Actions (move sync helpers to **`lib/utils/*`**). Root **`proxy.ts`** exports **`proxy()`** (not middleware) for security headers/metadata only—tenant/business resolution stays in Node route handlers (Prisma/pg), not in the proxy file. Hub routes **`/business/*`** and **`/purchases/*`** use server layouts (**`app/business/layout.js`**, **`app/purchases/layout.js`**) that **`getServerSession()`**-redirect to **`/login`** before hub chrome renders; client shell lives in **`BusinessShellLayout.jsx`**.
- **RSC / server actions → Client Components**: Prisma **`Decimal`** values are not serializable across the boundary (e.g. **`customers.credit_limit`**, **`outstanding_balance`**, **`opening_balance`**). Run payloads through **`serializeDecimalsDeep`** from **`lib/utils/serializePrismaDecimals.js`** before returning them to the client; do not rely on **`instanceof Prisma.Decimal`** alone when duplicate **`@prisma/client`** bundles can break type checks—use the helper’s decimal-like detection.
- Tenant-style isolation in this app is `business_id` scoped to `businesses`; there is no separate `tenant_id` column family across the main schema. **Forms and DB writes**: Prisma `findUnique` on tenant tables is **not** tenant-filtered by the extension—use `findFirst` with `business_id` and/or `assertEntityBelongsToBusiness` before mutations; multi-step writes should use transactions. See **`docs/DATA_INTEGRITY_AND_FORMS.md`**.
- Prisma migrations under `prisma/migrations` are the canonical database evolution path; `scripts/migrations` and `lib/db/migrations` are historical or manual—treat them as non-authoritative unless explicitly reconciled. Legacy `invoice_payments` rows may omit columns the app expects (`payment_method`, `received_by`, earlier `business_id` / soft-delete fields). Prefer Prisma **`20260606_invoice_payments_record_payment_columns`** and **`20260607_invoice_payments_received_by`**, or the idempotent SQL under `lib/db/migrations/038_invoice_payments_record_payment_columns.sql` and `039_invoice_payments_received_by.sql`, per **`docs/DATABASE_MIGRATIONS.md`**. Prisma maps `invoice_payments.received_by` / `deleted_by` as plain strings so values stay compatible with **`user`.id** (text).
- Customer AR balance truth uses **`invoice_payments`** and **`calculate_invoice_balance()`** (`InvoicePaymentService`, `PaymentModal`, API v1 invoice payments). Legacy **`payments`** + **`payment_allocations`** (`PaymentService`, `PaymentManager`) is a separate ledger path and does not reduce invoice balance unless explicitly routed through **`invoice_payments`**. Canonical payment types: **`lib/utils/paymentTypes.js`** (`receipt`/`payment`). **Purchase PO status** canon: **`lib/constants/purchaseStatus.js`** (`draft`/`ordered`/`received`/`cancelled`; **`normalizePurchaseStatus`** maps `sent`/`pending` → `ordered`) — use in Zod schemas, API routes, and PO/GRN UI.
- Hub inventory display stock comes from **`ProductService.resolveDisplayStock()`** (sum **`product_stock_locations`**, then batch/reserved qty, then variants/headline **`products.stock`**). Public storefront **display** stock mirrors hub logic via **`lib/storefront/storefrontDisplayStock.js`** (`enrichStorefrontProductStock`, `resolveStorefrontDisplayStock` — max of location qty, headline, variant sum) in **`lib/actions/storefront/products.js`**; cards/PDP/cart use **`storefrontStockUi.js`** and clothing attribute chips via **`productAttributeChips.js`**. Canonical **writes** use **`InventoryService`** (`product_stock_locations` → **`syncProductStock`**); **`batchAllocation.js`** / **`reservationManagement.js`** are unwired — FIFO/reservations live in **`InventoryService`**. Public storefront checkout (**`POST /api/storefront/[domain]/orders`**) decrements headline/variant stock via direct SQL and **bypasses** InventoryService (locations, movements, reservations). **`upsertIntegratedProductAction`** in **`lib/actions/premium/automation/inventory_composite.js`** must run stock add/remove on the **same pg client** (`invokeAddStockInTx` / `invokeRemoveStockInTx`); nested **`addStockAction`** opens another connection and can throw **Product not found** on uncommitted inserts. Placeholder serial rows must not force batch/serial mode; explicit headline **`stock`** is reconciled after batch/serial handling so inline grid edits persist.
- **Multi-country onboarding**: use **`lib/utils/regionalHelpers.ts`** (`getRegionalStandards`, `getRegistrationCountryOptions`) and **`docs/REGIONAL_STANDARDS.md`**. Registration stores `businesses.country` as the **canonical country name**; wizard/API may send **ISO or legacy aliases**. Extend **`REGIONAL_REGISTRY`** + **`ALIAS_TO_ISO`** before adding currencies in **`lib/currency/index.ts`**. Country-aware brand catalogs, payment gateways, and tax overlays resolve via **`lib/regionalMarket/`** (`getBrandsForMarket`, `getMarketFeatures`, **`domainBrandMap.js`**); hub callers use **`getDomainKnowledgeForBusiness(category, business)`** (not bare **`getDomainKnowledge(category)`**); pass **`countryIso`** into domain knowledge and run **`bun run verify:regional-market`** after catalog edits. **Runtime pack:** **`getBusinessRegionalPack`** (**`lib/utils/businessRegionalContext.js`**) merges registration, financials, and registry defaults; **`BusinessContext`** exposes **`regionalPack`** — hub/POS/print currency and tax labels must come from **`useBusiness()`**, not hardcoded PKR; public storefront currency/locale/tax via **`StoreProviders`** and **`lib/storefront/storefrontRegional.js`**; **`getPosUiConfig`** and thermal receipts use pack **`locale`**/**`taxIdLabel`**/**`defaultTaxRate`** over domain defaults. **Registration onboarding:** unified in **`lib/onboarding/*`** (`registrationStorefrontDefaults`, `registrationRichVerticals`, `registrationCategoryPresets`) + **`lib/utils/registrationSeed.js`** / **`RegistrationSeedService`**; **`createBusiness`** applies **`getDemoStorefrontProfile`**-derived storefront defaults for all domains, per-tenant **`store_payment_settings`**, then **`StorefrontSyncService.initializeStorefront`**; **`RegistrationInventoryBootstrap`** provisions primary warehouse + **`product_stock_locations`** for multi-location verticals; run **`bun run verify:registration-flow`** after edits. **Domain commercial packages:** **`lib/config/domainPackages.js`** (`DOMAIN_PACKAGES` — **`clothing-commerce`**, **`pharmacy-commerce`**, **`auto-parts-commerce`**, **`vehicle-showroom`**, **`furniture-commerce`**, **`fitness-commerce`**) with per-SKU **`settings.packaging`** overrides in **`lib/config/domainPackageFeatures.js`**; preset verticals + recommended plan + **`limitOverrides`** via **`buildRegistrationFromDomainPackage`** / **`createBusiness({ domainPackageKey })`**; marketing at **`/solutions/<slug>`** via **`DomainPackageSolutionsPage`** + unified **`DomainPackageHeroShowcase`** / **`buildUnifiedPackageSlides`**; register via **`?package=`**; runtime access still **`planHasFeatureWithPackaging`** — run **`npm run verify:domain-packages`** after edits.
- **Verticals (62+ keys)**: canonical presets live in **`lib/domainData/*.js`** → **`lib/domainKnowledge.js`**; each row should define **`intelligence`** (seasonality, peaks, perishability, lead time, etc.) for Industry Insights, Smart Restock, and AI forecasting. **`getDomainKnowledge`** merges defaults and supplies a **`name`** from the slug when the row omits it. Run **`bun run verify:domains`** after edits; see **`docs/DOMAIN_VERTICALS.md`**. Product **`domain_data`** reads/writes should resolve keys with **`normalizeKey(field)`** from **`lib/utils/domainHelpers.ts`** — normalize legacy aliases instead of dropping fields. Rich registration catalogs: global **`REGISTRATION_RICH_CATALOG_VERTICALS`** (`auto-parts`, `vehicle-dealership`, `gym-fitness`); PK clothing via **`PK_CLOTHING_REGISTRATION_VERTICALS`** (`garments`, `boutique-fashion`, `textile-wholesale`, `textile-mill`) gated by **`shouldSeedRichCatalogOnRegistration`** (PK country or **`clothing-commerce`** package) + **`lib/dataLab/pakistanClothingSeedCatalog.js`** (`domain_data.sourcing` local/imported); **`PACKAGE_RICH_CATALOG_VERTICALS`** (**`registrationRichVerticals.js`**) gates pharmacy/furniture/fitness-commerce package seeds; **`grocery`** is in **`SUPERMARKET_REGISTRATION_VERTICALS`**; other verticals get category shells + demo-profile storefront defaults. **Fashion editorial elevated store:** **`lib/storefront/fashionEditorial.js`**, **`FashionHomeSections`**, hero **`fashion-editorial`** (**`IMMERSIVE_FINDER_HERO_TYPES`**) for **`FASHION_EDITORIAL_CANONICALS`** only — **`FASHION_EDITORIAL_EXCLUDED`** (`textile-mill`, `gems-jewellery`) keep luxury styling, not editorial homepage sections; section toggles via **`settings.storefront.fashion`** / **`getFashionEditorialConfig`**; demo **`demo-boutique`** uses **`fullSeed: true`** in **`lib/dataLab/domains.mjs`** — run **`node scripts/verify-textile-storefront.mjs`** after edits. Domain aliases in **`lib/config/domainKeyAliases.js`**: **`apparel`** → **`garments`** (keep **`garments`** and **`textile-wholesale`** distinct); Pakistani brand packs via **`lib/domainData/pakistaniBrands.js`** / **`getBrandsForMarket`**. **Automotive storefronts (3 isolated keys):** **`auto-parts`** (integrated parts-finder hero — part/VIN/vehicle search; **`IMMERSIVE_FINDER_HERO_TYPES`**; registration seed via **`lib/dataLab/autopartsSeedCatalog.js`** + **`lib/storefront/autoPartsArchiveMap.js`** — run **`bun run verify:autoparts-seed`**), **`vehicle-dealership`** (single-brand — **`lib/storefront/autoDealership.js`**, **`dealershipShowroomProfile.js`**, **`components/storefront/sections/dealership/*`**; re-export **`isTenvoVehiclesShowroomProfile`** from **`autoDealership.js`**; buy/sell CTAs → booking/contact with owner info), **`auto-marketplace`** (portal — **`lib/storefront/autoMarketplace.js`**, **`components/storefront/sections/marketplace/*`**, aliases `car-marketplace`/`sgcarmart`; hero uses dark cinematic overlay in **`MarketplaceHero.jsx`** for legible white copy on bright vehicle photos). **Supermarket elevated store:** **`lib/storefront/supermarketStorefront.js`**, **`SupermarketHomeSections`**, hero **`supermarket-elevated`** / **`grocery-finder`** — vertical **`supermarket`** plus grocery/fmcg aliases; defaults **`supermarketCatalogDefaults.js`**, demo seed from **`archive/supper-store.html`**; showcase via **`resolveSupermarketShowcaseProducts`** in **`supermarketSeedHelpers.js`** (DB rows only + seed image enrich). **Restaurant elevated store (`restaurant-cafe` only):** **`lib/storefront/restaurantStorefront.js`**, **`restaurantCatalogDefaults.js`**, **`RestaurantHomeSections`**, hero **`restaurant-elevated`**, demo **`demo-restaurant`**; dedicated **`RestaurantSiteHeader`**, **`RestaurantChromeContext`**, **`RestaurantMobileBottomNav`** (not generic **`StoreMobileBottomNav`**). **Light-theme** homepage + digital menu at **`/products`** via **`lib/storefront/restaurantMenu.js`** + **`components/storefront/restaurant/*`** (unified header/search, category sidebar, grid/list, order-mode bar); dual theme tokens **`RESTAURANT_UI`** (dark) / **`RESTAURANT_CHECKOUT_UI`** (light cart/checkout pages) / **`RESTAURANT_CART_DRAWER_UI`** (**`CartDrawer`** — dark item list, light footer); order modes **delivery / takeaway (`collection`) / dine-in** persist per tenant in localStorage and sync cart → checkout → **`POST /api/storefront/[domain]/orders`** via **`normalizeRestaurantOrderMode`**, **`restaurantOrderModeToShipping`**, **`resolveRestaurantShippingCost`** (pickup/dine-in **$0**; server recalculates delivery fee); orders store **`metadata.restaurant_order_mode`** + kitchen **`notes`** (table, instructions); hub **`OrdersManager`** fulfillment card reads mode from metadata/notes. **DB-first orderable catalog:** **`buildRestaurantShopCatalog`** / **`resolveRestaurantShowcaseProducts`** in **`restaurantSeedHelpers.js`** — DB UUID rows only + seed image/category enrich (no **`catalog_preview`** in purchasable UI); category marquee images from product photos via **`resolveRestaurantCategoryVisual`**; remote eatx images via **`normalizeStorefrontRemoteImageUrl`** in **`lib/storefront/productImageFallback.js`** (**.avif** → **.jpg**). Demo catalog **Roll Inn** (not Tenvo Kitchen): **`lib/dataLab/restaurantDemoCatalog.js`** (~129 SKUs) from **`scripts/fetch-rollinn-menu.mjs`** + **`scripts/build-rollinn-restaurant-catalog.mjs`** (rollinnbbq.pk / eatx.pk API → **`rollinnArchiveExtract.json`**); demo refresh **`npx tsx scripts/data-lab/ensure-demo-storefronts.mjs --only demo-restaurant --refresh-catalog`** — run **`bun run verify:restaurant-seed`** after catalog edits. **Fashion/clothing rails:** OFFERS & SALE reverse marquee auto-scroll; ACCESSORIES / READY TO WEAR manual-only scroll; fixed slide widths **`lib/utils/storefrontProductRail.js`**; variant color/size via **`lib/storefront/storefrontProductVariants.js`**. **Pharmacy elevated store:** **`lib/storefront/pharmacyStorefront.js`**, **`components/storefront/sections/pharmacy/*`** — isolated from other templates. **Furniture elevated store:** **`lib/storefront/furnitureStorefront.js`**, **`components/storefront/sections/furniture/*`** — **`furniture`** vertical, hero **`furniture-elevated`**, demo **`demo-furniture`**; archive seed via **`lib/dataLab/furnitureDemoCatalog.js`** + **`furnitureArchiveExtract.json`** (from **`archive/furctinure1.html`** / **`furtiniture2.html`**). **Fitness elevated store:** **`lib/storefront/fitnessStorefront.js`**, **`components/storefront/sections/fitness/*`** — **`gym-fitness`** vertical, hero **`fitness-elevated`**, demo **`demo-fitness`**; archive seed via **`lib/dataLab/fitnessDemoCatalog.js`** (**`scripts/build-fitness-seed-catalog.mjs`** from **`archive/fitness-products.html`** + membership SKUs from **`archive/fitness.html`**) — run **`bun run verify:fitness-seed`** (not node — `@/` aliases) after catalog edits; demo refresh **`npx tsx scripts/data-lab/seed-master-demo.mjs --refresh-catalog --only demo-fitness`**. **`resolveFitnessShowcaseProducts`** enriches DB rows with seed images only — never replaces live catalog with seed SKU ids (`catalog_preview` when empty). **`/products` shop catalog** — **`isFitnessShopCatalogProduct`** / **`buildFitnessShopCatalog`** / **`paginateFitnessShopCatalog`** + **`getProducts({ fitnessShopCatalog: true })`** SQL filter in **`lib/actions/storefront/products.js`**; retail supplements/gear only — memberships, personal training, and classes stay on homepage booking sections; demo domains backfill retail seed SKUs via **`buildFitnessShopCatalog`**, live tenants use DB inventory only (no seed fill). **`FitnessMembershipPackages`** / **`resolveGymMembershipPackages`** for gents/ladies monthly–annual tiers; mobile **`FitnessMarqueeRow`** / **`fitness-training-marquee-track`** CSS auto-scroll below **`lg:`** (membership row reverse direction; desktop **`lg:grid`** unchanged). Hero **`FitnessHero.jsx`** ~52–58svh standard height. Tenant booking via **`settings.storefront.booking.meetingUrl`** (not platform Calendly). Vehicle listings use **`products`** + **`domain_data`** (make, model, condition, bodytype, fuel, etc.); public list filters accept **`brand`**/**`body`**/**`fuel`**/**`condition`** in **`lib/actions/storefront/products.js`**; marketplace hero/COE ticker is owner text in **`settings.storefront.marketplace`** (not a live LTA API).
- **Marketing demo stores & scheduling**: homepage gallery in **`DemoStoreGallery`** + **`ThreeDLayeredCard`** (`components/ui/3d-layered-card.jsx`); metadata/heroes from **`lib/marketing/demoStoreGalleryMeta.js`** — hero carousel via **`getHeroDemoGalleryItems()`** (**16** full-seed demos), card grid via **`getFeaturedDemoGalleryItems()`**, **`getDemoStoreHeroByDomain`** reused in **`CommerceAndIntelligenceSection`**; **`FEATURED_DEMO_STORES`** includes all **`DEMO_BUSINESS_PACK`** full-seed demos; **`HERO_EXCLUDED_DEMO_DOMAINS`**: `demo-mobile`, `demo-electronics`, `demo-solar`, `demo-salon` (showcase-only until elevated polish); honest homepage stat **16** in **`homeVisualThemes.js`**. Platform sales Calendly: **`lib/marketing/salesLinks.js`** (`getSalesMeetingUrl()` / **`NEXT_PUBLIC_SALES_MEETING_URL`**). Tenant meeting URLs: **`settings.storefront.booking.meetingUrl`** via **`lib/storefront/storefrontBooking.js`** / Store Settings when **`canConfigureTenantMeetingUrl`** (plan **`appointment_booking`** + booking verticals); never use platform Calendly for tenant storefront booking.
- **Market readiness / form wiring**: Hub forms should use **`lib/utils/formErrorHandler.jsx`** with **`actionSuccess` / `actionFailure`** from server actions; launch QA and scope are summarized in **`docs/MARKET_READINESS.md`** (not a substitute for end-to-end QA). Run **`bun run verify:mvp-launch`** after MVP launch wiring edits (OTP prod fail-loud, Stripe webhook **503** when unconfigured, Easy Mode **finance**/**gst** nav, auth confirmed onboarding redirect). **Easy mode dashboard:** **`DomainDashboard`** delegates to tabbed **`EasyBusinessDashboard`** when `isEasyMode`; domain playbooks/tab guidance in **`lib/dashboard/easyDomainIntelligence.js`**; stock KPIs and invoice sparklines via **`lib/dashboard/easyDashboardHelpers.js`** (`resolveProductStock`, `buildProductSparkHeights`); compact **`IndustryInsights`** variant — run **`bun run verify:easy-dashboard`** after edits. **Domain Operations tab** (Easy + Advanced): **`DomainOperationsPanel`**, **`domainOperationsIntelligence.js`** (vertical ops modes), **`getDomainOperationsSnapshotAction`** (`domainOperationsSnapshot.js`), contact queue **`updateStorefrontContactStatusAction`** (`storefrontContactMessages.js`) + **`domainOperationsSubjects.js`** (`STOREFRONT_CONTACT_SUBJECTS`, `CONTACT_PENDING_STATUSES`); snapshot uses **`getDomainKnowledgeForBusiness`** + **`getBusinessRegionalPack`**; hub contact queue omits email/phone/message body — run **`bun run verify:domain-operations`** after edits. **Platform billing:** Stripe plan/package checkout uses inline **`price_data`** from **`lib/payments/stripeCatalog.js`** (no pre-created Price IDs); offline payments via **`ManualPaymentRequestPanel`** + **`lib/payments/manualPaymentRequests.js`** → admin **`applyManualSubscriptionPayment`** (plan tier and **`domainPackageKey`**) — run **`bun run verify:manual-billing`** after billing edits. **NOWPayments crypto:** **`lib/payments/nowpayments.js`** (env **`NOWPAYMENTS_API_KEY`**, **`NOWPAYMENTS_IPN_SECRET`**, optional **`NOWPAYMENTS_IPN_CALLBACK_URL`**) — IPN **`POST /api/webhooks/nowpayments`** (recursively sort JSON keys → stringify → HMAC-SHA512 vs **`x-nowpayments-sig`**); SaaS via **`CryptoBillingPanel`** + **`POST /api/billing/crypto/create`** (order IDs **`tenvo-{businessId}-{timestamp}`**); storefront digital checkout via **`CryptoCheckoutPanel`** + **`POST /api/storefront/[domain]/crypto/create|status`** (order IDs **`store-{businessUuid}-{orderNumber}`**); crypto plan amounts share Stripe catalog via **`resolveBillableSku`**. Hub campaigns: honest capability copy in **`lib/marketing/campaignCapabilities.js`**; tenant integrations in **`lib/marketing/campaignIntegrations.js`** (`settings.campaigns.integrations`); run **`npm run verify:campaigns-hub`** after campaigns edits; scheduled outreach via **`POST /api/internal/campaigns/dispatch-scheduled`** needs **`CRON_SECRET`**. Default SaaS trial is **14 days** via **`TRIAL_CONFIG`** in **`lib/config/platform.js`** (`getTrialExpiry`, Stripe checkout trial). Admin platform feature flags use `platform_feature_flags` and `platform_feature_flag_overrides`. The Prisma `feature_flags` model (`business_id`, `feature_name`) exists but is **not** wired into hub plan guards today — **per-tenant module overrides** use `businesses.settings.packaging` via **`planHasFeatureWithPackaging`** / **`getPackagingFromSettings`** in **`lib/subscription/effectivePlanAccess.js`** (`PLAN_FEATURE_TOGGLE_KEYS` in **`lib/config/plans.js`**). Limit overrides live in **`settings.limit_overrides`** (**`lib/utils/businessLimitOverrides.js`**, **`PLAN_LIMIT_OVERRIDE_KEYS`**); platform admins edit in **`PlatformAdminPanel`**, owners read-only in Settings → Billing. Business **owners** edit packaging from **Settings → Billing** via **`updateOwnerBusinessPackagingAction`** (`settings.packaging` permission); platform staff use **`updateBusinessPackaging`** / **`updateBusinessLimitOverrides`** in `lib/actions/admin/platform.js`. API routes that accept **`businessId`** should use **`withApiAuth`** / **`withGuard`** (or equivalent membership check) before tenant data reads or mutations.
- **Hub mobile (below `lg`)**: tile hubs (`InventoryMobileHub`, `DashboardMobileHub`, `StorefrontMobileHub` + **`StorefrontTabShell`**); shared **`components/mobile/MobileHubPrimitives.jsx`**, form tokens **`lib/utils/formMobileStyles.js`**, FAB offset **`lib/utils/mobileLayout.js`** (`MOBILE_BOTTOM_NAV_CLASS`); **`HubMobileBottomNav`** + **`useHubMobileNav`**; child managers use **`useStorefrontEmbedded()`** inside the shell to skip duplicate headers — see **`docs/STOREFRONT_MOBILE_NAV.md`**. Public customer storefront mobile uses **`StoreMobileBottomNav`** in **`app/store/[businessDomain]/layout.jsx`** (deduped homepage sections, no duplicate headers/banners).
- Stripe webhooks and other cross-tenant billing writes should use `prismaBase` from `lib/db.js`, not the tenant-extended `db` client, especially for models like `stripe_webhook_events`. **`lib/db.js`** also documents pool TLS via `.env.example` (`DATABASE_SSL_*`); pool vs `prismaBase` vs tenant `db` tradeoffs in **`docs/AUDIT_SCHEMA_AND_INTEGRATIONS.md`**. **Production canonical host:** **`https://www.tenvo.store`** — production **`NEXT_PUBLIC_APP_URL`** must match for Stripe/NOWPayments webhooks and crypto return URLs. **Digital products:** flag on **`products.domain_data.fulfillment_type`** (**`digital`** and aliases via **`lib/storefront/digitalProducts.js`**); digital-only carts skip shipping at checkout. **Public storefront** uses raw `pool` SQL (not Prisma tenant extension): tenant APIs under **`/api/storefront/[businessDomain]/`** (cart sync, stock, orders — not legacy global paths); checkout **`POST /api/storefront/[domain]/orders`** ( **`createStorefrontOrder`** is deprecated), **`resolveStorefrontBusiness`** (Redis L2 before domain SQL), **`requireStorefrontHubAccess`** for hub actions; **`is_storefront_enabled`** lives on **`business_settings`** (**`COALESCE(bs.is_storefront_enabled, true)`**), not **`businesses`**; disabled tenants render **`StorefrontUnavailable`** via **`isStorefrontDisabledResult`** (not **`notFound()`**); homepage catalog batched via **`getStoreHomeCatalog`** / **`storeHomeCatalog.js`**; vertical sections code-split in **`LazyVerticalHomeSections`**; order numbers are per-tenant daily sequences via **`generateStorefrontOrderNumber`** (**`lib/utils/storefrontOrderNumber.js`**) with **`pg_advisory_xact_lock`** inside the checkout transaction; canonical uniqueness is **`(business_id, order_number)`** — also **`DROP INDEX IF EXISTS storefront_orders_order_number_key`** (orphan global index on **`order_number`** alone blocks cross-tenant **`ORD-YYYYMMDD-*`** with **409 "Checkout is busy right now"**; migrations **`20260704_storefront_order_number_unique`** + **`20260705_drop_storefront_order_number_global_index`**). **`POST /api/storefront/[domain]/orders`** returns immediately after **COMMIT**; emails/analytics/notifications/catalog invalidation run async via **`scheduleStorefrontOrderPostCommit`** (**`lib/storefront/storefrontOrderPostCommit.js`**). **`resolveStorefrontBusiness`** Redis L2 revalidates cached tenant against a live domain SQL row before trust. Cart sync errors return **503** **`{ synced: false }`** (not an empty cart). Owner homepage marketing slots: **`settings.pageSections`** (**`lib/storefront/storePageSections.js`**, **`StoreMarketingSections`**, max 12 sections). Stock/cart require **`businessId`** and canonical **`products.id`** UUIDs — **`lib/utils/storefrontProductRef.js`** (`isStorefrontProductUuid`, `resolveStorefrontProductId`) resolves UUID, case-insensitive SKU/barcode, **`product_variants.variant_sku`**, then slug in stock/cart APIs; run **`npm run verify:storefront-tenancy`** and **`npm run verify:storefront-domain-resolution`** after storefront edits. **Checkout intelligence (cross-domain):** **`lib/storefront/storefrontPurchasability.js`** (`isPurchasableStorefrontProduct`), **`storefrontCheckoutReconcile.js`** (`reconcileStorefrontCheckoutCart`), **`storefrontCartSanitize.js`**, **`validateCheckoutCart.js`** (SKU→UUID resolve + sole-variant auto-pick); cart reconcile on drawer/cart/checkout via **`syncCartFromReconcile`** in **`lib/context/CartContext.js`** — run **`bun run verify:storefront-checkout-intelligence`** after edits. **Storefront ops/analytics:** visitor KPIs via **`storefrontAnalytics.js`**, **`StorefrontAnalyticsBeacon`**, **`POST /api/storefront/[domain]/analytics`**, checkout revenue upsert; contact **`POST .../contact`** validates **`STOREFRONT_CONTACT_SUBJECTS`**, failed DB insert returns **503**; Prisma **`20260618_storefront_operations_hub`** + **`lib/db/migrations/043_storefront_operations_hub.sql`** for **`storefront_contact_messages`** and **`storefront_analytics.visitors`** — run **`verify:storefront-operations-db`** after schema/wiring edits; hub snapshot/contact status actions use **`withGuard`** + **`business_id`** scoping. Online sales fulfill via hub **orders** tab → **`OrdersManager`** / **`storefront_orders`** (not **`invoices`**). Commerce ledgers are separate: **`storefront_orders`**, **`pos_transactions`**, **`invoices`**, **`restaurant_orders`**. Finance flows that post journals should use `journal_entries` and `gl_entries` shapes from the schema (e.g. via `AccountingService`), not legacy column names from abandoned SQL sketches. **Hub POS:** **`resolvePosVariant(category)`** → **`RestaurantPOS`** (**`restaurant-cafe`**, **`restaurant_orders`** ledger), **`SuperStorePOS`** (supermarket/grocery/pharmacy/cold-storage/etc.), **`PosTerminal`** (other retail POS domains); mobile camera scan via shared **`PosCameraScanner`**. **`POS`** and other raw-SQL services must match Prisma column names for `pos_transaction_items` (e.g. `total_amount`, required `product_id`) to avoid runtime drift; domain document labels use **`getDomainConfig(category).label_overrides`** (not `.labels`) in POS/print/PDF. POS receipts: **`lib/print/thermalReceipt.js`** (table-based 58mm print/PDF), **`lib/hooks/usePosReceipt.js`**, one-page shell **`lib/utils/posLayout.js`**; checkout schema allows optional **`invoice_number`**, **`POSService`** writes **`payment_status`**/**`is_voided`**, line discounts included once in totals, **`AccountingService`** uses **`pos_sale`**/**`pos_refund`** entry types. Platform owner detection uses env allowlists (`PLATFORM_OWNER_EMAILS`). Hub **Pending Orders** in `RemindersPortlet` counts **invoice** pending/processing (not **`storefront_orders`**) and navigates to **`invoices`** (not **`purchases`**); map **`view-*`** quick actions via **`QUICK_VIEW_ACTION_TO_TAB`**. Alerts are split: in-app **`notifications`** + **`NotificationBell`**/SSE, inventory **`low_stock_alerts`** + dashboard KPIs, and Resend email — not a single unified pipeline.
